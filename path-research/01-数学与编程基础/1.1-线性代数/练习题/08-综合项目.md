# Ch8 综合项目（期末大作业）

> 目标：打通全部知识点，完成可展示的成品
> 产出：放到 `../../产出/` 目录下

---

## 🎯 项目 1：手写 PCA（核心项目）⭐⭐⭐ 🔥

**要求**：
- 不调用 `sklearn.decomposition.PCA`
- 至少实现两种方法：
  - 方法 A：协方差矩阵特征值分解
  - 方法 B：SVD 法
- 对结果一致性做验证

```python
class MyPCA:
    def __init__(self, n_components):
        self.n_components = n_components

    def fit(self, X):
        """
        X: (N, D) 数据矩阵
        1. 中心化：减去均值
        2. 计算协方差矩阵 或 直接 SVD
        3. 取前 k 个主成分
        保存：self.components_, self.explained_variance_, self.mean_
        """
        ...

    def transform(self, X):
        """投影到主成分空间 → (N, k)"""
        ...

    def inverse_transform(self, X_reduced):
        """从降维空间重建 → (N, D)"""
        ...

    def fit_transform(self, X):
        self.fit(X)
        return self.transform(X)
```

**验证**：
1. 生成 200 个 2D 正态分布点，`cov = [[3, 1.5], [1.5, 1]]`
2. 用 `MyPCA(n_components=1)` 降到 1 维
3. 画图：原数据散点 + 主成分方向箭头 + 重建后的点（落在主成分直线上）
4. 对比 `sklearn.decomposition.PCA`，`explained_variance_ratio_` 应一致

**进阶**：
- 用 MNIST 数据集（28×28=784 维），降到 2 维可视化，观察不同数字是否聚集
- 尝试降到 50 维再重建，观察重建质量

---

## 🎯 项目 2：SVD 图像压缩工具 ⭐⭐⭐

**要求**：一个 CLI 工具，支持：
```bash
python svd_compress.py --image photo.jpg --k 50 --output compressed.png
```

**功能**：
- 读入彩色图（RGB 三通道分别做 SVD）
- 保留前 k 个奇异值
- 输出压缩后的图像
- 打印压缩比和重建误差（Frobenius 范数）

**加分**：
- 画出 **奇异值衰减曲线**（对数坐标）
- 画出不同 k 值下的重建对比图（k=5, 20, 50, 100, 200）
- 生成一张 HTML 报告

---

## 🎯 项目 3：可视化线性变换 Playground ⭐⭐⭐

用 `matplotlib.animation` 或 `ipywidgets` 做一个交互式工具：

- 用户输入一个 2×2 矩阵
- 显示：
  - 单位正方形 → 变换后的平行四边形
  - 单位圆 → 变换后的椭圆（SVD 视角）
  - 标出特征向量方向（如果存在实特征值）
  - 显示 det、trace、rank、eigenvalues

**参考**：3Blue1Brown 的可视化风格

---

## 🎯 项目 4：LoRA 低秩近似实验 ⭐⭐⭐

**LLM 预热项目**：

```python
# 假设 W 是一个 4096×4096 的"预训练权重"
W = np.random.randn(4096, 4096)

# 造一个"微调方向" ΔW，人为制造成低秩
A_true = np.random.randn(4096, 8)
B_true = np.random.randn(8, 4096)
ΔW_true = A_true @ B_true  # rank 8

# 任务：
# 1. 对 ΔW_true 做 SVD，验证它确实是 rank 8（只有 8 个非零奇异值）
# 2. 如果只用 rank r 去近似它，r=1,2,...,16 重建误差怎么变化？
# 3. 参数量对比：
#    - 完整 4096×4096 = 16.7M
#    - rank-8 的 A, B 加起来 = ?
#    - 压缩比 = ?
```

**扩展思考**：为什么 LoRA 在 LLM 微调时 rank=8 或 16 就够用？

---

## 📊 项目产出要求

每个项目应该有：
- [ ] `README.md` — 项目说明
- [ ] 主代码文件（.py 或 .ipynb）
- [ ] 运行截图或结果图
- [ ] **复盘笔记** — 踩的坑、学到的东西

放到 `../../产出/项目N-XXX/` 目录下，例如：
```
产出/
├── 项目1-手写PCA/
│   ├── README.md
│   ├── my_pca.py
│   ├── demo.ipynb
│   └── results/
├── 项目2-SVD图像压缩/
│   └── ...
└── 项目3-变换可视化/
    └── ...
```

---

## 🏆 挑战赛（可选）

**在不调用任何线性代数库（只用 Python 标准库 + 纯数学运算）的情况下**，
实现一个可以跑通"MNIST 降维 + 最近邻分类"的完整管道：
- 手写矩阵类 `Matrix`
- 手写 SVD
- 手写 PCA
- 手写 KNN
- 准确率 > 90%

**意义**：能做出来说明你对线性代数的底层数值算法有真正的掌握。
