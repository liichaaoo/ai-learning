# 阶段 ⑨ · 压测 + GitHub 开源 + 写博客

> ⏱️ 1 天
> 🎯 给项目打上"性能可信"的标签 + 让世界看到它

---

## 0. 任务清单

- [ ] k6 / wrk 压测：QPS / P50 / P95 / 错误率
- [ ] GitHub Repo 开源 + README 完善
- [ ] 录 5 ~ 30 分钟演示视频
- [ ] 写一篇技术博客（推荐：思否 / 掘金 / 公司内部）
- [ ] 把项目链接 + 数字补到简历

---

## 1. k6 压测脚本

```javascript
// scripts/load-test.js
import http from 'k6/http';
import { check, sleep } from 'k6';

export const options = {
  stages: [
    { duration: '30s', target: 20 },
    { duration: '1m',  target: 50 },
    { duration: '30s', target: 0 },
  ],
  thresholds: {
    http_req_duration: ['p(95)<2000'],
    http_req_failed:   ['rate<0.01'],
  },
};

const QS = ['公司晋升流程', '年假怎么算', 'OKR 评分规则', '出差报销', '入职须知'];
const TOKEN = __ENV.TOKEN;

export default function () {
  const q = QS[Math.floor(Math.random() * QS.length)];
  const res = http.post(
    'http://localhost:8080/api/sessions/1/chat',
    JSON.stringify({ q }),
    { headers: { 'Content-Type': 'application/json', 'Authorization': `Bearer ${TOKEN}` } }
  );
  check(res, { 'status 200': r => r.status === 200 });
  sleep(1);
}
```

跑：

```bash
TOKEN=xxxx k6 run scripts/load-test.js
```

输出：

```
http_req_duration..............: avg=1.23s p(50)=1.18s p(95)=1.62s
http_reqs......................: 4502     74.2/s
http_req_failed................: 0.02%
```

> 🎯 **简历数字**：「k6 压测 50 并发，单实例 P95 < 1.6s，错误率 < 0.1%」

---

## 2. GitHub Repo README 模板

```markdown
# Enterprise RAG Knowledge Base

[![License](...)] [![Build](...)]

> 基于 Spring AI + Milvus 的企业知识库 RAG 系统，支持多租户 / 多模型 / 流式问答 / 来源引用

## ✨ 特性

- 📚 多格式文档：PDF / Word / Markdown / HTML
- 🔍 混合检索：向量召回 + BM25 + Reranker（Recall@3 = 90%）
- 💬 流式问答 + 来源引用（页码定位）
- 🧠 多模型路由：通义 / GPT / Ollama 按场景路由（成本节省 ~40%）
- 👥 多租户隔离 + JWT 鉴权
- 📊 Token 用量统计 + 成本告警
- 🐳 Docker Compose 一键部署

## 🏗 架构

![architecture](docs/architecture.png)

## 🚀 快速开始

```bash
git clone https://github.com/yourname/enterprise-rag-kb
cd enterprise-rag-kb
cp .env.example .env  # 填入 DASHSCOPE_API_KEY
docker compose up -d
open http://localhost:8080
```

## 📊 性能数据

| 指标 | 值 |
|---|---|
| Top-3 召回率 | 90%（30 题评测）|
| P95 延迟 | 1.6s |
| 单实例 QPS | 50+ |
| 月度成本节省 | ~40%（通过路由）|

## 🎥 演示

[5 分钟演示视频](https://youtube.com/...) / [博客](https://mp.weixin.qq.com/...)

## 📂 文档

- [架构详解](docs/architecture.md)
- [调优实验](docs/调优实验报告.md)
- [API 文档](docs/api.md)

## 📜 License

MIT
```

---

## 3. 技术博客大纲（4000~5000 字）

```
1. 我为什么做这个？
   - 业务痛点（公司知识散落，搜索效果差）
   - 技术兴趣（学完 Spring AI 想落地）

2. 整体架构
   - 一张架构图（务必）
   - 关键技术栈选型理由

3. RAG 核心机制
   - 文档解析 → 分片 → 向量化 → 入库（一段流程图）
   - 混合检索三段论：召回 → RRF → Rerank
   - 防幻觉的 system prompt 设计

4. 工程化亮点
   - 多租户 metadata 隔离
   - 多模型路由策略
   - Token 计费 Advisor
   - 异步入库 + 进度查询

5. 调优实验
   - 评测集设计
   - 对比表 + 折线图
   - 最终配置

6. 性能压测
   - k6 报告截图

7. 踩坑记录
   - chunk_size 选 200 时召回崩了
   - bge-m3 vs 通义 v3 对比
   - Milvus 维度对不上的低级错误

8. 后续规划
```

---

## 4. 简历更新（最关键的一步）

把项目从"模糊描述"升级为"数字驱动":

```diff
- 用 Spring AI 做了一个 RAG 项目，支持企业知识库问答。
+ 主导设计基于 Spring AI + Milvus 的企业 RAG 知识库系统（个人项目，开源 GitHub）：
+   • 实现混合检索（向量召回 + BM25 + Rerank），Top-3 召回率从 65% → 90%（30 题人工评测）
+   • 多模型路由（通义 / GPT-4o / Ollama），月度推理成本节省 ~40%
+   • 单实例 P95 1.6s，支撑 50+ QPS（k6 压测）
+   • Docker Compose 一键部署 / 多租户 / Token 用量计费
+   • 链接：github.com/yourname/enterprise-rag-kb
```

> 💡 **简历投出去前**，对照 [`../../05-简历与面试/简历/对标简历参考.md`](../../05-简历与面试/简历/对标简历参考.md) 检查数字是否完整、有竞争力。

---

## 5. 验收标准

- [ ] k6 报告截图存到 `docs/perf-report.png`
- [ ] GitHub Repo 公开 + README 完整 + Star ≥ 1（自己点一下也行）
- [ ] 演示视频上传到 B 站 / YouTube / 内网
- [ ] 博客发出来，至少有一个公开链接
- [ ] **简历更新完毕**，数字埋点对齐

---

## 🎉 阶段 3 通关

恭喜！你已经拥有了一个**能写进简历、能讲 30 分钟、能拿出截图的真实项目**。

### 这意味着

- ✅ 简历从"会用 Spring AI" → "做过简历级 RAG 项目"
- ✅ 面试从"看 ppt 提问" → "看 GitHub 提问"
- ✅ 涨薪谈判时多一份硬资本

### 下一站

- ➡️ [阶段 4 · Agent 与工程化](../../04-Agent与工程化/README.md)（让 AI 不止能"答"还能"做"）
- 📋 [阶段 5 · 简历与面试](../../05-简历与面试/README.md)（开始投递）

---

## 🔗 相关链接

- ⬆️ [Week 5-6 总览](./README.md)
- ⬅️ [阶段 ⑧](./08-部署与前端.md)
- ➡️ [阶段 4 · Agent 与工程化](../../04-Agent与工程化/README.md)
- 📋 [对标简历参考](../../05-简历与面试/简历/对标简历参考.md)
